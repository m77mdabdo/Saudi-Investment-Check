APP_NAME="Saudi-Ready Check"
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost

# Arabic is the default language; English is served from /en/...
APP_LOCALE=ar
APP_FALLBACK_LOCALE=ar
APP_FAKER_LOCALE=en_US
# Let a first-time visitor's browser language pick the site language.
LOCALE_AUTO_DETECT=false

APP_MAINTENANCE_DRIVER=file

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=saudi_investment_check
DB_USERNAME=root
DB_PASSWORD=

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null
SESSION_SECURE_COOKIE=true

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database
CACHE_STORE=database

# ── Mail (SMTP) ────────────────────────────────────────────────
# Lead alerts + customer result emails. Never commit real values.
MAIL_MAILER=smtp
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_ENCRYPTION=tls
# Seconds to wait for the SMTP conversation. Raise it on slow shared hosting.
MAIL_TIMEOUT=30
MAIL_FROM_ADDRESS="no-reply@example.com"
MAIL_FROM_NAME="${APP_NAME}"

# Keep exactly ONE block of MAIL_* keys in this file. A duplicate key later in
# the file silently overrides the one above it.

# ── Pexels (background imagery) ────────────────────────────────
# Optional. Without it the app serves the bundled fallback images.
PEXELS_API_KEY=
PEXELS_CACHE_TTL=86400
PEXELS_TIMEOUT=4

# ── Google OAuth (staff sign-in only) ──────────────────────────
# Leave OAUTH_PROVIDERS empty to hide the Google button entirely.
OAUTH_PROVIDERS=google
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_CALLBACK_URL="${APP_URL}/auth/google/callback"
GOOGLE_ALLOWED_DOMAINS=
GOOGLE_AUTO_REGISTER=false

# ── Public CTA destinations (also editable in Admin → Settings) ─
CREATIVE_MARK_WHATSAPP_URL=
BOOKING_URL=
CHECKLIST_URL=
CREATIVE_MARK_PHONE=
CREATIVE_MARK_EMAIL=
CREATIVE_MARK_WEBSITE=

# ── Lead notifications ─────────────────────────────────────────
LEAD_NOTIFY_ADMIN=true
LEAD_NOTIFY_CUSTOMER=true
LEAD_NOTIFY_EMAILS=
# All three are also editable in Admin → Settings → Notifications.

# ── First admin account (used once by db:seed) ─────────────────
ADMIN_NAME="Creative Mark Admin"
ADMIN_EMAIL=admin@creativemark.local
# Leave ADMIN_PASSWORD empty to have a strong one generated and printed once.
ADMIN_PASSWORD=

# ── Branding ───────────────────────────────────────────────────
BRAND_NAME="Creative Mark"
BRAND_TAGLINE="Creating The Future"
BRAND_LOGO=images/creative-mark-logo.png

VITE_APP_NAME="${APP_NAME}"

# Comma separated proxy IPs/CIDRs, or * behind a trusted load balancer.
TRUSTED_PROXIES=
