*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/hot
/storage/*.key
/storage/pail
/vendor
Homestead.json
Homestead.yaml
Thumbs.db
.env.backup*

# ── Deployment (Hostinger serves the project root) ────────────────────────
# public/build   → COMMITTED on purpose: the server has no Node build step.
# public/storage → COMMITTED symlink so uploaded media works after a pull.
# .htaccess      → COMMITTED: without it the domain answers 403.
